<p>
  	<strong>Title:</strong>
 	<%= @video.title %>
 </p>

<p>
	<strong>Text:</strong>
	<%= @video.text %>
</p>

<h2>Comments</h2>
<%= render @video.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_video_path(@video) %> |
<%= link_to 'Back', videos_path %>
